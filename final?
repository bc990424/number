a= 1
b=100
d=[2]
def privenum(x,y):
  star= x if a%2 ==1 else x+1         #시작숫자 홀수로 샐성
  for i in range(star,y-x,2):         #숫자열 생성
    d.append(i)
  d.append(2)                         # 2 추가
  try:
    del d[1]                          #1 삭제
  except:
    pass
  for num in d:
    for m in range(3,num-1):
      if num%m==0:                    #나누어 떨어지면
        d.remove(num)                 #삭제
        break        
privenum(a,b)                         #함수호출
print(d)
